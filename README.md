=======================================

TriAngels Universal Terminal Standard

=======================================

Этот файл настраивает единый профессиональный терминал
для iMac / MacBook / NAS / VPS (Linux).

Система автоматически определяет:

Тип операционной системы (macOS или Linux)

root или обычный пользователь

SSH или локальное подключение

Наличие Docker

Тип shell (bash или zsh)

После установки вы получите:

Цветное отображение пользователя

Имя машины (разным цветом для macOS и Linux)

IP-адрес устройства

Docker-индикатор 🐳

Профессиональный DevOps-вид

Единый стандарт TriAngels

Что НЕ делает этот файл

Не трогает Docker

Не перезапускает сервисы

Не меняет SSH

Не изменяет firewall

Не влияет на WireGuard

Не ломает ноды

Он меняет только внешний вид терминала.

====================
ИНСТРУКЦИЯ УСТАНОВКИ
====================

## Установка одной командой (рекомендуется)

### Безопасный вариант (скачать файл → проверить → запустить)

**macOS / Linux / NAS / VPS**

## ⚡ One-Command Install

```bash
curl -fsSL https://raw.githubusercontent.com/Aleks250483/triangels-universal-terminal/main/setup-triangels-universal.sh | bash
```
chmod +x setup-triangels-universal.sh
./setup-triangels-universal.sh

После установки:

- Перезапусти терминал или

- Выполни:

  - source ~/.bashrc (bash)

  - source ~/.zshrc (zsh)

_____________________________

Обновление (повторный запуск)
_____________________________

Скрипт безопасно обновляет конфигурацию:

- делает backup ~/.config/starship.toml

- делает backup ~/.bashrc / ~/.zshrc при изменениях

- обновляет только свой блок по маркеру TRIANGELS_TERMINAL_STANDARD
________________

Удаление / откат
________________

rm -f ~/.config/starship.toml
# Верни rc-файлы из резервных копий: ~/.bashrc.bak.<timestamp> или ~/.zshrc.bak.<timestamp>

Примечание: скрипт не трогает Docker, не меняет firewall,
не влияет на VPN/WireGuard — меняет только внешний вид терминала.



# Вариант 1 — Установка на VPS / Linux сервер
_____________________________________________

Подключитесь к серверу:

ssh root@ВАШ_IP

Загрузите файл на сервер (если он на вашем компьютере):

scp setup-triangels-universal.sh root@ВАШ_IP:/root/

Подключитесь к серверу:

ssh root@ВАШ_IP

Сделайте файл исполняемым:

chmod +x setup-triangels-universal.sh

Запустите установку:

./setup-triangels-universal.sh

Перезапустите терминал
или выполните:

source ~/.bashrc

Готово.

# Вариант 2 — Установка на macOS (iMac / MacBook)
_________________________________________________

Откройте Terminal.

Перейдите в папку, где находится файл:

cd ~/Downloads

Сделайте файл исполняемым:

chmod +x setup-triangels-universal.sh

Запустите:

./setup-triangels-universal.sh

Если система попросит установить Homebrew —
следуйте подсказкам на экране.

Перезапустите Terminal.

Готово.

# Вариант 3 — Установка на NAS (TerraMaster / Linux NAS)
________________________________________________________

Подключитесь по SSH:

ssh USER@IP_NAS

Загрузите файл:

scp setup-triangels-universal.sh USER@IP_NAS:/home/USER/

Подключитесь к NAS:

ssh USER@IP_NAS

Дайте права:

chmod +x setup-triangels-universal.sh

Запустите:

./setup-triangels-universal.sh

Перезапустите SSH-сессию.

Готово.
___________________________________

Если нужно вернуть стандартный вид

Удалите файл конфигурации:

rm ~/.config/starship.toml

Затем перезапустите терминал.

Результат
_________

macOS → синий хост
Linux VPS → зелёный хост
root → красный
обычный пользователь → cyan
Docker → 🐳 индикатор
IP → отображается

===================================================================
Теперь все устройства команды выглядят одинаково и профессионально.
===================================================================

Версия

TriAngels Terminal Standard v1.0
Infrastructure Bootstrap Edition
